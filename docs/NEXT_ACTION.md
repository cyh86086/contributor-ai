# Next action

Last reviewed: 2026-08-02
Execution baseline: resolve the live `main` SHA during mandatory preflight.

## Active task

**Task ID:** `D16-DEVICE-PROCEDURE-PREPARATION`

**Objective:** Prepare the separately governed, evidence-only D16 device
procedure and launcher package from the clarified formal contract and proved
offline aggregate semantics, without executing a phone or claiming D16 PASS.

The completed test-only coverage proves the 10-iteration orchestration,
fail-fast counters, metadata equality, UI precedence, and one frozen sanitized
aggregate through the existing production reader and portable core. It is
offline evidence only and does not establish Android, AutoJs6, provider,
temporary-picker-grant lifetime, device behavior, or D16 PASS.

## Required work

1. Complete mandatory preflight and re-read the formal D16 contract, both D16
   reviews, the test-only aggregate coverage, production reader/core/reporter
   path, and device-verification governance before changing any procedure or
   launcher.
2. Confirm the private fixture mapping and independently remeasure the approved
   `JPEG_REPEAT_VALID` fixture outside Git, the launcher, and evidence. Retain
   only the approved opaque fixture ID and independently verified positive byte
   count in repository material.
3. Define a deterministic evidence-only procedure that selects the fixture once
   with the Android system picker, uses one fresh temporary grant, and performs
   exactly 10 complete production reader/core iterations without reselection.
4. Reuse the approved aggregate semantics: exact counters, iteration-1
   MIME/count equality, fail-fast, one loop-level responsiveness assessment,
   failure precedence, and one frozen allowlisted report.
5. Prepare only the minimal reviewed AutoJs6 launcher, generated bundle, and
   Traditional Chinese user procedure required by repository governance. Keep
   the picker callback and image-reading loop off the UI thread and preserve all
   stable public failures unchanged.
6. Run all authoritative repository checks and scope/privacy scans, then publish
   the preparation change for independent review. Do not execute it on a phone
   during this task.

## Acceptance criteria

- The preparation package follows the formal D16 contract and the proved
  offline aggregate semantics without inventing a classification or acceptance
  rule.
- It uses the system picker, one fresh temporary grant, the existing production
  reader and portable core, exactly 10 complete iterations, and one sanitized
  aggregate report.
- Repository material contains no URI, path, filename, image content, bytes,
  Base64, exception detail, stack, credential, or private fixture mapping.
- The procedure clearly distinguishes preparation from device evidence and
  requires a later, separately governed user-assisted execution.
- The full authoritative test suite and all formatting, bundle, syntax,
  privacy, and scope checks pass, and the change is independently reviewed.

## Prohibited scope

Do not execute a phone, use the Android picker, create device evidence, or claim
D16 PASS in this preparation task. Do not change the production reader, portable
core, aggregate semantics, permissions, or application architecture.

Do not add persistable access, broad storage permission, a permission manager,
provider, network, queue, Contributor app, credential, submission, source-copy
architecture, or unrelated module work.

## Stop conditions

Stop and report when:

- another pull request or branch already owns device-procedure preparation;
- the formal contract, fixture provenance, independently verified count, or
  required launcher behavior is ambiguous;
- preparation requires phone execution, real device evidence, sensitive values,
  production architecture changes, or prohibited permissions;
- required checks fail outside the authorized preparation scope;
- GitHub write access is unavailable.
